# Single, evolving ESP-IDF project for the self-balancing vehicle.
# We grow this one project feature-by-feature as we work through the roadmap.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(balance_bot)
